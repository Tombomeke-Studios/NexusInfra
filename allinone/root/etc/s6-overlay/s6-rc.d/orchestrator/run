#!/command/with-contenv sh
set -a
. /run/nexusinfra.env
set +a
exec 2>&1
# db:deploy applies the SQLite or PostgreSQL migrations, whichever DATABASE_URL names (#241).
exec sh -c 'cd /app/services/orchestrator && npm run db:deploy && node dist/index.js'

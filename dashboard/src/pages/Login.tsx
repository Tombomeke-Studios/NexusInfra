// Placeholder — the real login form (credentials → POST /auth/login → store
// token) lands in its own unit (#30).
export function Login() {
  return (
    <main>
      <h1>Sign in</h1>
      <p>Login form coming next.</p>
    </main>
  );
}
